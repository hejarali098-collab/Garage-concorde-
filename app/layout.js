import "./globals.css";

export const metadata = {
  title: "Garage Concorde - Pointage",
  description: "Pointage des employés"
};

export default function RootLayout({ children }) {
  return <html lang="fr"><body>{children}</body></html>;
}
